# DarvisXBot 订阅会员加密支付接入归档

记录时间：2026-07-29

## 当前结论

订阅会员支付功能先延后开发。原因是本地阶段还没有公网 HTTPS 回调地址，NOWPayments 无法把付款状态通知到本地服务。建议等 DarvisXBot 全部核心代码开发完成并成功部署到公网服务器后，再接入支付通道。

当前会员菜单已经有套餐入口：

- 10U 1个月
- 29U 3个月
- 55U 6个月
- 100U 1年

但目前只是菜单按钮和套餐说明，没有接真实支付、订单、到账回调和自动开通会员。

## 已排除方案：Crypto Bot / Crypto Pay

最开始考虑使用 Crypto Bot 的 Crypto Pay API，因为它和 Telegram Bot 场景比较贴近。

实际测试结果：

- 打开 `@CryptoTestnetBot`
- 点击 `Set Up Account`
- 点击 `Share Phone Number`
- 账号被提示 `Your account is suspended`
- 原因是手机号地区不支持

截图提示的核心意思：

```text
Your region is not supported.
Access to your account has been permanently restricted because it is registered with a phone number from an unsupported country.
```

结论：

Crypto Bot / Crypto Pay 不能作为当前项目的主支付通道。它依赖 Telegram 账号和手机号地区，存在账号地区限制、账号封禁后资金访问受影响等风险。

参考：

- Crypto Pay API: https://help.send.tg/en/articles/10279948-crypto-pay-api
- Crypto Bot 不可用地区: https://help.send.tg/en/articles/12460759-unavailable-regions-in-crypto-bot
- Telegram 账号丢失后的钱包访问风险: https://help.send.tg/en/articles/9819513-what-if-i-can-t-access-my-telegram-account

## 推荐方案：NOWPayments

后续推荐使用 NOWPayments 做外部加密货币收款。

原因：

- 不依赖 Telegram 手机号地区
- 支持 USDT 等常见币种
- 支持 API 创建支付订单
- 支持 IPN / Webhook 通知付款状态
- 更像传统商户支付平台，适合 Bot 外部收款

官方资料：

- 官网: https://nowpayments.io/
- API 文档: https://nowpayments.io/api
- 创建账号: https://nowpayments.io/help/about-nowpayments/how-to-start/how-to-create-an-account
- 创建 API Key: https://nowpayments.io/help/dashboard/how-to-create-an-api-key
- 添加钱包: https://nowpayments.io/help/dashboard/how-to-add-a-crypto-wallet
- IPN 说明: https://nowpayments.io/help/what-is/what-is-ipn

## NOWPayments 已完成事项

已注册 NOWPayments 账号，并进入后台。

已进入：

```text
Settings -> Payments -> Instant payment notifications
```

已生成：

```text
IPN secret key
```

注意：IPN secret key 不要发到聊天里，只放到服务器 `.env`。

## NOWPayments 暂未完成事项

以下事项等部署前或部署后再做：

1. 开启 2FA。
2. 添加 USDT 收款钱包，建议优先使用 USDT-TRC20。
3. 生成 API Key。
4. 准备公网 HTTPS 回调地址。
5. 在 NOWPayments 后台保存 Webhook URL。

## 为什么现在不能填 Webhook URL

曾检查过：

```text
https://doingfb.com/api/nowpayments/ipn
```

当前返回 `404 Not Found`，说明该接口还不存在。

NOWPayments 的 Webhook URL 必须是真实可访问的后端接口，不能随便填一个域名路径，也不能填本地地址，例如：

```text
http://localhost:3000/payments/nowpayments/ipn
```

正确做法是先让 DarvisXBot 在公网服务器部署成功，并真正提供一个接口，例如：

```text
POST https://你的域名/payments/nowpayments/ipn
```

然后再把这个地址填到 NOWPayments 后台。

## 后续推荐接入流程

等项目部署完成后，按这个流程接入：

1. 用户点击会员套餐按钮。
2. 后端创建本地支付订单。
3. 后端调用 NOWPayments API 创建支付。
4. Bot 返回支付链接按钮。
5. 用户完成 USDT 支付。
6. NOWPayments 请求项目的 IPN / Webhook 地址。
7. 后端校验 IPN 签名。
8. 后端校验订单金额、币种、状态。
9. 后端把订单标记为已支付。
10. 后端给用户开通或顺延会员有效期。
11. Bot 给用户发送开通成功通知。

## 建议新增环境变量

后续接入 NOWPayments 时，建议在 `.env` 中增加：

```env
NOWPAYMENTS_API_BASE=https://api.nowpayments.io/v1
NOWPAYMENTS_API_KEY=你的_API_KEY
NOWPAYMENTS_IPN_SECRET=你的_IPN_SECRET
PUBLIC_BASE_URL=https://你的域名
```

如果要额外保护回调地址，也可以增加：

```env
NOWPAYMENTS_WEBHOOK_PATH_SECRET=一段随机字符串
```

示例回调地址：

```text
https://你的域名/payments/nowpayments/ipn/一段随机字符串
```

## 建议新增数据模型

后续建议不要直接复用现有 `Membership` 表来表示 Bot 高级会员。当前 `Membership` 更像是用户与某个群组或频道的会员关系。

建议新增 Bot 自身高级会员模型，例如：

```prisma
model BotSubscription {
  id        String   @id @default(cuid())
  userId    String   @unique @map("user_id")
  expiresAt DateTime @map("expires_at")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("bot_subscriptions")
}
```

支付订单建议单独建表：

```prisma
model PaymentOrder {
  id                String    @id @default(cuid())
  telegramUserId    BigInt    @map("telegram_user_id")
  provider          String    @default("nowpayments")
  planKey           String    @map("plan_key")
  months            Int
  amountUsd         Decimal   @map("amount_usd") @db.Decimal(18, 8)
  payCurrency       String?   @map("pay_currency")
  providerPaymentId String?   @unique @map("provider_payment_id")
  payUrl            String?   @map("pay_url")
  status            String    @default("PENDING")
  rawPayload        Json?     @map("raw_payload")
  paidAt            DateTime? @map("paid_at")
  createdAt         DateTime  @default(now()) @map("created_at")
  updatedAt         DateTime  @updatedAt @map("updated_at")

  @@index([telegramUserId, status])
  @@map("payment_orders")
}
```

## 后续代码改造范围

预计需要修改或新增：

- `prisma/schema.prisma`
- `.env.example`
- `src/lib/config.ts`
- `src/server.ts`
- `src/telegram/bot.ts`
- `src/payments/nowpayments.service.ts`
- `src/payments/payment-order.service.ts`
- `src/subscriptions/subscription.service.ts`

## 支付安全要求

正式接入时必须做到：

- 不把 API Key、IPN Secret 写入代码。
- 不在聊天记录里发送真实密钥。
- IPN / Webhook 必须校验签名。
- 支付订单必须幂等处理，重复回调不能重复开通会员。
- 必须校验金额，例如 10U 套餐必须对应 10 USD。
- 必须校验订单 ID，不能只根据付款金额开会员。
- 只在最终确认状态后开通会员。
- 支付失败、过期、少付、多付要有明确状态记录。
- 会员到期时间应该顺延，不要直接覆盖已有剩余时长。

## 下次恢复开发时的第一步

先确认项目已经部署成功，并且公网可以访问健康检查接口，例如：

```text
GET https://你的域名/health
```

然后再新增并测试：

```text
POST https://你的域名/payments/nowpayments/ipn
```

确认公网接口可访问后，再回 NOWPayments 后台填写 Webhook URL。
